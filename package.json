{
  "name": "mitou-optimizer",
  "version": "1.0.0",
  "description": "MITOU IT project application document generator",
  "main": "worker.ts",
  "scripts": {
    "generate-data": "node generate-sections-data.js",
    "validate-data": "node validate-data.js",
    "prebuild": "npm run generate-data",
    "build": "npm run generate-data && npm run validate-data",
    "dev": "npm run generate-data && wrangler dev",
    "deploy": "npm run generate-data && npm run validate-data && wrangler deploy",
    "postinstall": "npm run generate-data"
  },
  "keywords": [
    "mitou",
    "cloudflare",
    "workers"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231025.0",
    "pdfjs-dist": "^5.4.530",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
